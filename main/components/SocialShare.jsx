"use client"

import React from 'react';

const SocialShare = ({ title ="title", content="content" }) => {
  const titleToShare = `Check out this amazing post: ${title}`;

  return (
      <></>
  );
};

export default SocialShare;