const config = {
    siteUrl: 'https://www.encuentralofacilcr.com',
    generateRobotsTxt: true, // (Optional parameter for creating robots.txt file)
    sitemapSize: 7000,
  }
 
module.exports = config;